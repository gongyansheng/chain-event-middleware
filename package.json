{
  "name": "chain-event-middleware",
  "version": "1.0.16",
  "description": "",
  "files": [
    "dist",
    "package.json"
  ],
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -fr dist && tsc --declaration",
    "deploy": "npm run build; npm version patch; npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@ton/ton": "^13.11.1",
    "bignumber.js": "^9.1.2",
    "ethers": "^5.7.2"
  }
}
